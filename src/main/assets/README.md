Place the window icon here.

Recommended file:

- `app-icon.ico`

The app will use `src/main/assets/app-icon.ico` first. If you want the official VS Code icon, download `resources/win32/code.ico` from the Microsoft VS Code repository and replace this file.
